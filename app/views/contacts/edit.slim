h3 = t('phonebook.hints.update', first_name: @contact.first_name, last_name: @contact.last_name)
= simple_form_for @contact, html: { class: 'pure-form pure-form-aligned' } do |f|
  fieldset
    = render partial: 'form', locals: { f: f}